# 2026-03-11

## 邮件发送 Markdown 内联样式优化

`email-send.ts` 的 `mdToHtml` 函数增加了 `injectEmailStyles` 后处理步骤，为 Markdown 转换后的 HTML 标签（h1-h3、p、ul/ol/li、blockquote、code/pre、a、table/th/td、hr）注入邮件客户端兼容的内联样式，确保邮件正文中的 Markdown 内容（标题、列表、代码块、表格等）在各邮件客户端中正确美观地渲染。

## 工作流画布 Dify 风格优化

参考 Dify 画布交互体验，对 `workflow-canvas/` 进行了全面升级：

### 新增文件
- **`useEdgeDrag.ts`** - 拖拽连线 hook，状态机管理（端口检测 via `elementFromPoint`、画布坐标转换、连接触发）
- **`useCanvasKeyboard.ts`** - 快捷键 hook（Delete 删除节点/连线、Escape 取消选中、Space 画布拖拽模式）
- **`DragPreviewLine.tsx`** - 拖拽连线预览组件（虚线贝塞尔曲线+发光效果+鼠标圆点）
- **`Minimap.tsx`** - Canvas 2D 小地图组件（缩略节点/连线、视口范围框、点击/拖拽快速定位）

### 改造文件
- **`canvas-utils.ts`** - 新增 `PORT_SIZE`、`getInputPortPos/getOutputPortPos/getStartOutputPortPos` 端口坐标计算、`canConnect` 连接校验
- **`StepNode.tsx`** - 添加 input/output 端口圆点 UI（hover 放大高亮）、端口 pointerdown 触发连线拖拽、isDropTarget 高亮
- **`EdgeLines.tsx`** - 连线 hover 高亮+发光效果、选中态流动动画（stroke-dasharray）、中点删除按钮
- **`WorkflowCanvas.tsx`** - 集成 useEdgeDrag/useCanvasKeyboard/DragPreviewLine，开始节点增加输出端口
- **`WorkflowEditorPage.tsx`** - 新增 handleConnect/handleDeleteEdge 回调、集成 Minimap（含 ResizeObserver 跟踪容器尺寸）

## create-craft 输入解析修复

`create-craft.ts` 修复了上游步骤传递 JSON 对象时无法识别的问题。原代码只支持从 `_params` 字段中提取 JSON 字符串再 `JSON.parse`，但工作流引擎通过 `Object.assign(merged, prevData)` 传递上游结果时，`ctx.input` 已经是一个包含 craft 字段（name、htmlCode 等）的对象，而非 JSON 字符串。重写后支持三种输入格式：1) 直接 JSON 字符串；2) 对象中含 JSON 字符串字段；3) 对象本身就是 craft 数据。新增 `isCraftLike` 判断、`extractCraftFields` 字段提取（排除 `_action`/`_params` 等元字段）、`parseJsonToItems` JSON 解析三个辅助函数。

## manage-workflow 技能 — 增删改重构

重写了 `manage-workflow.ts`，从仅支持创建变为支持三种操作（create / update / delete）：

### 参数变更（skills.ts）
- 新增 `action`（select：🆕 新增工作流 / ✏️ 修改工作流 / 🗑️ 删除工作流，必填）
- 新增 `workflowId`（select，从 API `/api/workflows/team/:teamId` 异步加载当前团队工作流列表，修改/删除时必选）
- 保留 `prompt`（textarea，`valueSource: 'upstream'`）
- 删除 `teamId`（改为自动从 URL 或用户团队列表获取，只操作自己的团队）

### 三种操作实现
1. **create**：获取团队猫猫信息 → 调用 AI 生成工作流配置 → `POST /api/workflows/team/:teamId`
2. **update**：先 `GET /api/workflows/:id` 获取现有工作流 → 拼接修改需求+现有信息+猫猫信息 → AI 重新生成 → `PUT /api/workflows/:id`
3. **delete**：获取工作流名称 → `DELETE /api/workflows/:id`

### StepParam 类型扩展（types.ts）
新增三个可选字段支持 select 参数动态加载选项：
- `asyncOptionsFrom`：API 路径（如 `/api/workflows/team/:teamId`，`:teamId` 运行时从 URL 替换）
- `asyncOptionsValueKey`：选项 value 取值字段，默认 `'id'`
- `asyncOptionsLabelKey`：选项 label 取值字段，默认 `'name'`

### StepConfigPanel UI 支持（StepConfigPanel.tsx）
新增 `useAsyncOptions` Hook，在 `ParamInput` 的 `select` 分支中：
- 检测 `param.asyncOptionsFrom` 是否存在
- 存在时调用 API 加载选项，显示「加载中...」状态
- 自动替换路径中的 `:teamId` 占位符
- 异步选项和静态 `options` 互斥使用

## 发发（fafa）角色重塑 — 从质量审核员到创意策划师

将发发从 QA Reviewer 重塑为 Creative Strategist（创意策划师），赋予 AI 驱动的头脑风暴方法论技能体系。

### 新增三个 Skill Handler
- **`mece-analysis.ts`**（🧩 MECE 问题拆解法）：按"相互独立、完全穷尽"原则将复杂问题拆解为多层子问题树，输出含 dimensions/subQuestions/meceCheck/nextSteps 的结构化 JSON
- **`scamper-creative.ts`**（🔀 SCAMPER 创意改造）：从 S/C/A/M/P/E/R 七个维度对产品或方案进行创意发散，每个维度含创意点+可行性评分(1-5)+影响+effort，最终选出 Top 3
- **`six-hats.ts`**（🎩 六顶思考帽）：按白→红→黑→黄→绿→蓝顺序从六种思维视角分析问题，蓝帽做全局总结+行动计划

三个 handler 均基于 `structured-output` 原型，通过 `executePrimitive` 调用 LLM。

### 技能注册（skills.ts）
- 在 `skillPool` 中注册三个 `SkillTemplate`（category: `content`，各自带一个 `topic` textarea paramDef，`valueSource: 'upstream'`）
- 将 skillGroup `qa`（质量审核员）改为 `creative`（创意策划师），icon 💡，color #FFB74D，skillIds 为三个新 skill

### 角色信息更新（fafa.ts）
- role: `Creative Strategist`，description 更新为创意策划师定位
- accent 改为 `#FFB74D`（橙色，体现创意活力）
- systemPrompt 完全重写，性格设定为思维活跃、联想丰富、善于发散
- fafaSkills 替换为三个头脑风暴技能
- fafaMessages 更新为创意主题气泡消息
- id 保持 `'reviewer'` 以兼容数据库

## AI 最大返回 token 数扩大 — 修复 JSON 截断问题

AI 生成结构化数据（如 MECE 拆解、SCAMPER 创意、六顶思考帽等）时，因 `maxTokens` 默认值 2048 过小导致 JSON 输出被截断、解析失败。

### 变更
- **`backend/routes/dify.js`**：
  - `callQwen` 默认 `maxTokens` 从 2048 → 4096
  - `TASK_MAX_TOKENS` 映射表扩展，所有结构化输出类 skill（workflow-gen, mece-analysis, scamper-creative, six-hats, generate-outline, content-review, recruit-cat, team-review, cat-training）设为 8192
  - 其余未列出的 taskId 默认 4096
- **`backend/workflow-executor.js`**：`callQwen`、`callGemini`、`callAI` 三个函数默认 `maxTokens` 均从 2048 → 4096
- **`backend/prisma/schema.prisma`**：`TeamCat.maxTokens` 默认值从 2048 → 4096
