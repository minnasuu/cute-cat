# 2026-03-09 工作日志

## 修复 Docker 部署后数据库不存在导致无法登录的问题
- **问题**：每次重新 docker 部署后，`cute_cat` 数据库可能不存在，导致 Prisma 迁移失败、后端无法启动、用户无法登录
- **根因**：`POSTGRES_DB` 环境变量仅在 PostgreSQL 容器首次初始化数据卷时创建数据库；如果数据卷被删除或在其他环境全新部署，数据库可能不会被创建。`entrypoint.sh` 之前没有检查数据库是否存在就直接执行迁移
- **修改文件**：
  - `backend/Dockerfile`：新增 `RUN apk add --no-cache postgresql-client` 安装 psql/pg_isready 工具
  - `backend/entrypoint.sh`：重写启动脚本，新增三个阶段：1) 解析 DATABASE_URL 提取连接参数 2) 等待 PostgreSQL 就绪（最多30次重试） 3) 检查并自动创建目标数据库 4) 执行 Prisma 迁移（含 baseline fallback）
  - 还补充了第三个迁移 `20260308000000_add_workflow_fields` 的 resolve 逻辑

## 修复 frontend npm run build 失败问题
- **问题**：`vite build` 时 Rollup 无法解析 `chart.js` 模块（未安装），导致构建失败
- **根因**：`chart-render.ts` 中使用 `await import('chart.js')` 动态导入，虽有 try/catch 兜底，但 Rollup 仍会静态分析字符串字面量的动态导入路径并尝试解析
- **修复**：将 `import('chart.js')` 改为 `Function('m', 'return import(m)')('chart' + '.js')`，Rollup 无法静态分析变量拼接的模块名，跳过解析，运行时仍能正常动态加载
- **修改文件**：`frontend/src/skills/primitives/chart-render.ts`

## 工作流执行弹窗添加步骤耗时 & 总耗时
- **需求**：显示每个步骤的耗时，footer 显示整体耗时
- **实现**：
  - 新增 `stepTimings` Map 状态（`start` + `duration`），在每步开始时记录 start，完成时算 duration
  - 新增 `totalElapsed` 状态 + `setInterval` 每秒更新实时总耗时
  - 每步结果头部右侧显示 `⏱ x.xs`
  - footer 执行中状态显示实时跑动的秒表，完成后显示最终总耗时
  - `updateRunRecord` 将每步 `duration` 保存到后端 run.steps 中
  - 查看历史记录时从 `run.steps[].duration` 恢复 stepTimings 展示每步耗时
- **修改文件**：`frontend/src/pages/TeamDetailPage/index.tsx`
